/**
 * Test stub for the `server-only` package (Sprint 012).
 *
 * `server-only` is a Next.js build guard that throws if imported into a client
 * bundle. It has no meaning under Vitest, so it is aliased to this no-op module
 * (see vitest.config.ts) to let server-only modules be unit-tested.
 */
export {};
